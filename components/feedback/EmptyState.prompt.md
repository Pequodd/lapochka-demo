Empty state for a filtered-out catalogue.

```jsx
<EmptyState onAction={resetFilters} />
```

The heading is display-m — coral-600 is legal here because the size clears 24px. Copy stays warm and slightly self-deprecating («вкусов у нас всё равно 24»), never an error message.
