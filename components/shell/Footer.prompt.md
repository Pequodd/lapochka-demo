Site footer — the same on all three screens.

```jsx
<Footer />
```

Contents are fixed: info@ / promo@ / sales@ with a Russian note each, socials, «стать партнёром» as an underlined coral-700 link, policy links at 14px, then a 140px marquee in coral-500 on peach-400. The page's last wave divider must land on peach-400 so the footer continues the section colour.
